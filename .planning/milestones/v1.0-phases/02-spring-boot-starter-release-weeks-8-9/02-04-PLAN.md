---
phase: 02-spring-boot-starter-release-weeks-8-9
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetStarterIntegrationTest.kt
  - build.gradle.kts
  - README.md
  - .github/workflows/release.yml
autonomous: true
requirements:
  - SPRING-01
  - SPRING-02
  - SPRING-03
  - REL-01
user_setup: []

must_haves:
  truths:
    - "A @SpringBootTest boot smoke test hits GET /actuator/budget and asserts gen_ai.* metrics appear in the MeterRegistry after a tracked call (D-19 layer 2)"
    - "The starter is included in the nmcp aggregation so it publishes alongside core"
    - "The root version is 0.1.0 (core + starter released together at one version)"
    - "README has a Spring Boot section: dependency, application.yml, @Autowired usage, actuator/metrics output"
  artifacts:
    - path: "budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetStarterIntegrationTest.kt"
      provides: "@SpringBootTest smoke test for endpoint + metrics"
      contains: "@SpringBootTest"
    - path: "build.gradle.kts"
      provides: "version 0.1.0 + starter in nmcpAggregation"
      contains: "nmcpAggregation(project(\":budget-breaker-spring-boot-starter\"))"
    - path: "README.md"
      provides: "Spring Boot starter usage section"
      contains: "budget-breaker-spring-boot-starter"
  key_links:
    - from: "build.gradle.kts allprojects"
      to: "version 0.1.0"
      via: "uniform version bump"
      pattern: "version = \"0.1.0\""
    - from: "nmcpAggregation dependencies"
      to: "budget-breaker-spring-boot-starter"
      via: "aggregation project entry"
      pattern: "nmcpAggregation\\(project\\(\":budget-breaker-spring-boot-starter\"\\)\\)"
    - from: "BudgetStarterIntegrationTest"
      to: "GET /actuator/budget + gen_ai meters"
      via: "@SpringBootTest asserts endpoint + MeterRegistry"
      pattern: "/actuator/budget"
---

<objective>
Close the phase: prove the assembled starter end-to-end with a @SpringBootTest smoke test (D-19 layer 2), then prepare the v0.1.0 release by adding the starter to the nmcp aggregation, bumping the uniform version to 0.1.0 (D-12/D-14), documenting the Spring Boot integration in the README (D-15), and updating the release workflow notes to mention both artifacts.

Purpose: SPRING-01/02/03 are individually unit-tested in earlier plans; this plan verifies they compose in a real Spring context and that the published bundle will contain both jars at one version. README-only demonstration per D-15 (no examples/ app ships).

Output: One integration test, root build edits (version + aggregation), README Spring section, release.yml body update.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-PATTERNS.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-01-SUMMARY.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-02-SUMMARY.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols CREATED by this plan (new — exclude from drift/source-grounding checks):
- `BudgetStarterIntegrationTest` (test, @SpringBootTest) — boot smoke test
- `build.gradle.kts`: version bump to 0.1.0; `nmcpAggregation(project(":budget-breaker-spring-boot-starter"))` entry
- `README.md`: new "## Spring Boot" section
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: @SpringBootTest boot smoke test (D-19 layer 2, SPRING-02 + SPRING-03)</name>
  <files>budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetStarterIntegrationTest.kt</files>
  <read_first>
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt (beans available to autowire: BudgetCircuitBreaker; MetricsEventCollector + endpoint registered)
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt (exact meter names gen_ai.client.token.usage.input/output; lifecycle start() launches collection)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetCircuitBreaker.kt (withBudget(agentId){ trackCall(...) }; getReport)
    - budget-breaker/src/test/kotlin/io/github/unityinflow/budget/BudgetCircuitBreakerTest.kt (runTest/runBlocking usage + trackCall pattern, concurrent agent test lines 128-147)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (@SpringBootTest smoke test sketch; Pitfall 6 collector-before-events — ensure SmartLifecycle started; A1 SimpleMeterRegistry lives in micrometer-core)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-19 layer 2: one @SpringBootTest hitting /actuator/budget and asserting gen_ai.* metrics in MeterRegistry after a tracked call)
  </read_first>
  <behavior>
    - Boot a minimal @SpringBootTest application that imports BudgetBreakerAutoConfiguration with a real MeterRegistry on the context
    - After running breaker.withBudget("test-agent") { trackCall(promptTokens, completionTokens) }, the MeterRegistry has a gen_ai.client.token.usage.input counter tagged agent=test-agent with the expected count
    - The budget endpoint operation (or GET /actuator/budget via MockMvc if web layer is enabled) reports the test-agent entry
  </behavior>
  <action>
    Create `BudgetStarterIntegrationTest.kt` as a `@SpringBootTest`. Provide a minimal test `@SpringBootConfiguration`/`@EnableAutoConfiguration` (or `@ContextConfiguration` importing `BudgetBreakerAutoConfiguration`) plus a `SimpleMeterRegistry` bean so Micrometer is present (SimpleMeterRegistry is in micrometer-core per RESEARCH A1). Autowire the `BudgetCircuitBreaker`, the `MeterRegistry`, and the `MetricsEventCollector`. Ensure the collector is started before emitting (SmartLifecycle auto-starts with the context, but if needed call `start()` explicitly and allow the SharedFlow buffer to absorb events; yield/await briefly so the collector subscribes before asserting — mirror the yield pattern from BudgetCircuitBreakerTest). In a runBlocking/runTest block, call `breaker.withBudget("test-agent") { trackCall(promptTokens = 1000, completionTokens = 500) }`. Then assert via the MeterRegistry that the counter named "gen_ai.client.token.usage.input" with tag agent=test-agent has count 1000.0 (use `registry.get("gen_ai.client.token.usage.input").tag("agent","test-agent").counter().count()`), and the .output counter is 500.0. Also assert the budget endpoint sees the agent: call the autowired `BudgetEndpoint.budget()` (or `breaker.getReport("test-agent")`) and assert it contains "test-agent". If enabling the web layer for a real GET /actuator/budget, set `webEnvironment = RANDOM_PORT` with `management.endpoints.web.exposure.include=budget` via @TestPropertySource and use TestRestTemplate/MockMvc; otherwise asserting through the endpoint bean + MeterRegistry satisfies D-19 layer 2. Account for Pitfall 6: do not emit before the collector subscribes — await subscription. Use coroutines test utilities; no Thread.sleep.
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache --tests "io.github.unityinflow.budget.spring.BudgetStarterIntegrationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@SpringBootTest" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetStarterIntegrationTest.kt` returns 1
    - `grep -c "gen_ai.client.token.usage.input" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetStarterIntegrationTest.kt` returns >=1
    - `grep -Ec "test-agent" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetStarterIntegrationTest.kt` returns >=1
    - `grep -c "Thread.sleep" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetStarterIntegrationTest.kt` returns 0
    - `./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache --tests "io.github.unityinflow.budget.spring.BudgetStarterIntegrationTest"` passes
  </acceptance_criteria>
  <done>A real Spring context boots the starter; tracked-call tokens surface as gen_ai.* meters and the agent appears in the budget endpoint.</done>
</task>

<task type="auto">
  <name>Task 2: Add starter to nmcp aggregation + bump version to 0.1.0 (REL-01)</name>
  <files>build.gradle.kts, .github/workflows/release.yml</files>
  <read_first>
    - build.gradle.kts (current: allprojects version = "0.0.1" at line 12; nmcpAggregation block lines 23-30; dependencies { nmcpAggregation(project(":budget-breaker")) } lines 32-34; comment lines 19-21 say starter is excluded)
    - .github/workflows/release.yml (release body template lines 51-64 mentions only budget-breaker; publishingType USER_MANAGED note)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (Root build.gradle.kts nmcp additions example; Pitfall 3 aggregation name collision — publicationName uses version, fine)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-12 tag v0.1.0 core+starter together; D-14 add starter to nmcp aggregation + it applies the publishing convention)
  </read_first>
  <action>
    In root `build.gradle.kts`: change `version = "0.0.1"` to `version = "0.1.0"` in the `allprojects` block (D-12 uniform version). In the `dependencies` block add `nmcpAggregation(project(":budget-breaker-spring-boot-starter"))` directly after the existing `nmcpAggregation(project(":budget-breaker"))` line (D-14). Update the explanatory comment above the nmcpAggregation block (currently says the starter is excluded because it is an empty stub) to state that BOTH modules are now published as of v0.1.0. Do NOT change publicationName (it interpolates project.version → "budget-breaker-0.1.0", correct per Pitfall 3). Do NOT add or hardcode any secrets — SONATYPE_USERNAME/PASSWORD/SIGNING_KEY/SIGNING_PASSWORD stay as environmentVariable lookups / GitHub secrets. In `.github/workflows/release.yml`: update the release body template (the `body: |` block) to mention both artifacts — add an `implementation("io.github.unityinflow:budget-breaker-spring-boot-starter:${{ steps.version.outputs.version }}")` snippet alongside the core dependency, and note that 0.1.0 adds Spring Boot support. Keep `runs-on: [arc-runner-unityinflow]` and `--no-configuration-cache` unchanged. Do not alter the secrets env mapping.
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:publishToMavenLocal --no-configuration-cache && ls ~/.m2/repository/io/github/unityinflow/budget-breaker-spring-boot-starter/0.1.0/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'version = "0.1.0"' build.gradle.kts` returns 1 AND `grep -c 'version = "0.0.1"' build.gradle.kts` returns 0
    - `grep -c 'nmcpAggregation(project(":budget-breaker-spring-boot-starter"))' build.gradle.kts` returns 1
    - `grep -c 'nmcpAggregation(project(":budget-breaker"))' build.gradle.kts` returns 1
    - `grep -Ec "SONATYPE_PASSWORD|SIGNING_KEY" build.gradle.kts | grep -v environmentVariable` does not introduce literal secrets (no hardcoded key values present)
    - `grep -c "budget-breaker-spring-boot-starter" .github/workflows/release.yml` returns >=1
    - `./gradlew :budget-breaker-spring-boot-starter:publishToMavenLocal --no-configuration-cache` produces `budget-breaker-spring-boot-starter-0.1.0.jar` plus sources/javadoc jars under ~/.m2
  </acceptance_criteria>
  <done>Starter is in the nmcp aggregation, version is 0.1.0 uniformly, release notes mention both artifacts, no secrets committed; publishToMavenLocal emits the starter artifact set.</done>
</task>

<task type="auto">
  <name>Task 3: README Spring Boot section (D-15)</name>
  <files>README.md</files>
  <read_first>
    - README.md (current structure: problem statement, installation, usage examples, API reference, model pricing table — find where to add the Spring Boot section)
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt (the documented knobs: default-model, hard-limit-tokens, soft-limit-tokens, pricing map)
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt (routes GET /actuator/budget and /{agentId})
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-15 README-only demo: dependency snippet, application.yml example, @Autowired usage, actuator/metrics output; D-16 document that per-agent budgets stay code-side via withBudget(budget=...))
  </read_first>
  <action>
    Add a "## Spring Boot" section to README.md. Include: (1) a dependency snippet `implementation("io.github.unityinflow:budget-breaker-spring-boot-starter:0.1.0")` noting it transitively brings the core; (2) an `application.yml` example showing the `budget-breaker.*` knobs — `default-model`, `hard-limit-tokens`, `soft-limit-tokens`, and a `pricing` override map with one model's `input-per-million`/`output-per-million` — plus `management.endpoints.web.exposure.include: health,budget` (document opt-in exposure; do not imply auto-exposure); (3) an `@Autowired` usage example injecting ONLY the `BudgetCircuitBreaker` bean (D-17) and calling `withBudget`; (4) note that per-agent budgets are code-side via `withBudget(budget = AgentBudget(...))` — no per-agent yml map in this release (D-16); (5) sample `GET /actuator/budget` JSON output and the list of emitted Micrometer metric names (`gen_ai.client.token.usage.input`/`.output`, `budget.breaker.breach`, `budget.breaker.usage.ratio`, `budget.breaker.cost.usd`). Note the health indicator always reports UP with agentsTracked/softBreaches/hardBreaches details. Spring Boot baseline 3.5.x (works on 3.4+). Keep prose accurate to the shipped behavior — do not document deferred features (no webhook, no PAUSE, no per-agent yml).
  </action>
  <verify>
    <automated>grep -c "budget-breaker-spring-boot-starter" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Spring Boot" README.md` returns 1
    - `grep -c "budget-breaker-spring-boot-starter:0.1.0" README.md` returns >=1
    - `grep -c "management.endpoints.web.exposure.include" README.md` returns >=1
    - `grep -c "gen_ai.client.token.usage.input" README.md` returns >=1
    - `grep -Ec "alert-webhook|PAUSE|budget-breaker.agents" README.md` returns 0 (no deferred features documented as shipped)
  </acceptance_criteria>
  <done>README documents the starter: dependency, application.yml knobs, @Autowired breaker usage, opt-in actuator exposure, metric names, and the code-side per-agent budget note — no deferred features claimed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI release.yml → Sonatype Central Portal | signed artifact bundle crosses to public registry |
| build files / CI → secrets | credentials must come from env vars / GitHub secrets only |
| consuming app → /actuator/budget | documented exposure boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-09 | Information Disclosure | secrets leaking into build.gradle.kts / release.yml | mitigate | Credentials stay as providers.environmentVariable(...) (build) and ${{ secrets.* }} (CI); grep gate asserts no literal secret values added |
| T-02-10 | Tampering | unsigned or wrong-version artifact published | mitigate | budget-breaker.publishing convention GPG-signs via SIGNING_KEY/PASSWORD; uniform version 0.1.0; publishToMavenLocal verifies artifact set before tagging |
| T-02-11 | Information Disclosure | README implying /actuator/budget is auto-exposed | mitigate | README documents opt-in via management.endpoints.web.exposure.include (Spring default keeps budget unexposed) |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; all deps Maven-coordinate and org-verified (RESEARCH Package Legitimacy Audit, all OK) |
</threat_model>

<verification>
- `./gradlew build --no-configuration-cache` — full multi-module build incl. starter unit + integration tests green
- `./gradlew :budget-breaker-spring-boot-starter:publishToMavenLocal --no-configuration-cache` — starter artifact set produced at 0.1.0
- `./gradlew ktlintCheck --no-configuration-cache` after ktlintFormat
- README + release.yml mention both artifacts; no secrets committed
</verification>

<success_criteria>
- @SpringBootTest smoke test green: endpoint + gen_ai.* metrics after a tracked call (D-19 layer 2)
- Root version 0.1.0; starter in nmcp aggregation; publishToMavenLocal emits the starter jar set
- release.yml notes mention both artifacts; no literal secrets in build/CI
- README has accurate Spring Boot section; no deferred features documented as shipped
- Full `./gradlew build` green
</success_criteria>

<output>
Create `.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-04-SUMMARY.md` when done
</output>
