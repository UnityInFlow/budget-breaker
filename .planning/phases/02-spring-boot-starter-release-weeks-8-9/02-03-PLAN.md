---
phase: 02-spring-boot-starter-release-weeks-8-9
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt
  - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt
  - budget-breaker-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports
  - budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfigurationTest.kt
autonomous: true
requirements:
  - SPRING-01
user_setup: []

must_haves:
  truths:
    - "Auto-config registers exactly one app-facing BudgetCircuitBreaker bean, guarded by @ConditionalOnMissingBean"
    - "Auto-config also registers SLF4JEventLogger, MetricsEventCollector, BudgetEndpoint, and BudgetBreakerHealthIndicator beans"
    - "SLF4JEventLogger logs soft-limit breaches at WARN via SLF4J, subscribing to the core events Flow"
    - "The auto-config is discoverable via the AutoConfiguration.imports file (not spring.factories)"
    - "ApplicationContextRunner tests prove bean registration, user-bean override, and fail-fast on invalid properties"
  artifacts:
    - path: "budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt"
      provides: "@AutoConfiguration with @EnableConfigurationProperties and five @Bean methods"
      contains: "@AutoConfiguration"
    - path: "budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt"
      provides: "SmartLifecycle WARN logger over SoftLimitReached events"
      contains: "SmartLifecycle"
    - path: "budget-breaker-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports"
      provides: "Auto-config registration line"
      contains: "io.github.unityinflow.budget.spring.BudgetBreakerAutoConfiguration"
    - path: "budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfigurationTest.kt"
      provides: "ApplicationContextRunner unit tests for SPRING-01 conditionals + validation"
      contains: "ApplicationContextRunner"
  key_links:
    - from: "BudgetBreakerAutoConfiguration"
      to: "BudgetBreakerProperties"
      via: "@EnableConfigurationProperties + @Bean budgetCircuitBreaker(properties)"
      pattern: "@EnableConfigurationProperties"
    - from: "budgetCircuitBreaker @Bean"
      to: "@ConditionalOnMissingBean"
      via: "user-override guard"
      pattern: "@ConditionalOnMissingBean"
    - from: "imports file"
      to: "BudgetBreakerAutoConfiguration"
      via: "fully-qualified class name line"
      pattern: "BudgetBreakerAutoConfiguration"
---

<objective>
Wire the starter together: create the SLF4JEventLogger (D-03 WARN logging) and the BudgetBreakerAutoConfiguration that registers all beans from properties (SPRING-01, D-17/D-18), register it via the AutoConfiguration.imports file, and prove it with ApplicationContextRunner unit tests (D-19 layer 1).

Purpose: This is the wiring layer. It consumes the leaf components from plan 02 (BudgetBreakerProperties, BudgetEndpoint, BudgetBreakerHealthIndicator, MetricsEventCollector) and the core enhancement from plan 01 (BudgetCircuitBreaker constructor, ModelPricing). After this plan, a Spring Boot app that depends on the starter gets a fully auto-configured, overridable BudgetCircuitBreaker plus observability — zero config.

Output: Two source files, the imports registration file, and the auto-config unit test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-PATTERNS.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-01-SUMMARY.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols CREATED by this plan (new — exclude from drift/source-grounding checks):
- `SLF4JEventLogger` (SmartLifecycle) — `start`, `stop`, `isRunning`
- `BudgetBreakerAutoConfiguration` (@AutoConfiguration) — @Bean methods: `budgetCircuitBreaker`, `budgetEventLogger`, `budgetMetricsCollector`, `budgetEndpoint`, `budgetHealthIndicator`
- `AutoConfiguration.imports` registration file (resources)
- `BudgetBreakerAutoConfigurationTest` (test, ApplicationContextRunner)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: SLF4JEventLogger (WARN logging on soft-limit breach, D-03)</name>
  <files>budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt</files>
  <read_first>
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetEvent.kt (SoftLimitReached fields: agentId, tokensUsed, budgetTokens, percentUsed; plus HardLimitExceeded, CallTracked)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetCircuitBreaker.kt (events: SharedFlow<BudgetEvent>)
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt (plan-02 analog: same SmartLifecycle + CoroutineScope(SupervisorJob()+Dispatchers.Default) pattern to mirror)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (Pattern 6 SmartLifecycle coroutine scope; Pitfall 6 collector-before-events)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-03: log soft-limit breaches at WARN via SLF4J)
  </read_first>
  <action>
    Create `SLF4JEventLogger.kt` implementing `org.springframework.context.SmartLifecycle`. Constructor: `private val breaker: BudgetCircuitBreaker`. Hold `private val log = LoggerFactory.getLogger(SLF4JEventLogger::class.java)` and `private val scope = CoroutineScope(SupervisorJob() + Dispatchers.Default)`. `override fun start()` launches `scope.launch { breaker.events.collect { event -> when(event) { is BudgetEvent.SoftLimitReached -> log.warn("Agent '{}' reached soft budget limit: {}/{} tokens ({}%)", event.agentId, event.tokensUsed, event.budgetTokens, event.percentUsed); is BudgetEvent.HardLimitExceeded -> {}; is BudgetEvent.CallTracked -> {} } } }`. The when MUST be exhaustive over all three variants (no else) — hard/tracked are intentional no-ops per D-03 (only soft is logged). `override fun stop()` sets running=false and scope.cancel(). `override fun isRunning(): Boolean`. Mirror the exact lifecycle pattern used by MetricsEventCollector in plan 02. Never use `var` for the running flag — use an AtomicBoolean or a private mutable backing via Atomic; never `!!`. KDoc the class. No @Component.
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "SmartLifecycle" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt` returns >=1
    - `grep -c "log.warn" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt` returns 1
    - `grep -Ec "is BudgetEvent.SoftLimitReached|is BudgetEvent.HardLimitExceeded|is BudgetEvent.CallTracked" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt` returns 3
    - `grep -c "GlobalScope" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt` returns 0
    - `grep -c "!!" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/SLF4JEventLogger.kt` returns 0
    - `./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache` exits 0
  </acceptance_criteria>
  <done>SLF4JEventLogger logs only SoftLimitReached at WARN, subscribes via the events Flow, and manages its scope via SmartLifecycle with an exhaustive when.</done>
</task>

<task type="auto">
  <name>Task 2: BudgetBreakerAutoConfiguration + imports registration file (SPRING-01)</name>
  <files>budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt, budget-breaker-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</files>
  <read_first>
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetCircuitBreaker.kt (constructor: defaultBudget: AgentBudget, pricing: ModelPricing, onSoftLimit callback — auto-config builds it from properties; D-18 does NOT wire onSoftLimit)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/AgentBudget.kt (constructor model/hardLimitTokens/softLimitTokens)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/ModelPricing.kt (constructor: overrides: Map<String, PriceConfig>; PriceConfig(inputPerMillion, outputPerMillion))
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt (plan 02: defaultModel, hardLimitTokens, softLimitTokens, pricing: Map<String, ModelPriceProperties>)
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt, BudgetBreakerHealthIndicator.kt, MetricsEventCollector.kt (plan 02 beans this config registers)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (Pattern 1 @AutoConfiguration + @EnableConfigurationProperties + @Bean @ConditionalOnMissingBean; imports file location; Pitfall 5 @Endpoint must be a @Bean; anti-pattern: no MeterRegistry in constructor)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-17 ONE app-facing bean BudgetCircuitBreaker with @ConditionalOnMissingBean; D-18 no onSoftLimit bean; D-02 pricing map mapping)
  </read_first>
  <action>
    Create `BudgetBreakerAutoConfiguration.kt`: annotate the class `@AutoConfiguration` and `@EnableConfigurationProperties(BudgetBreakerProperties::class)`. Add `@Bean @ConditionalOnMissingBean fun budgetCircuitBreaker(properties: BudgetBreakerProperties): BudgetCircuitBreaker` that builds `BudgetCircuitBreaker(defaultBudget = AgentBudget(model = properties.defaultModel, hardLimitTokens = properties.hardLimitTokens, softLimitTokens = properties.softLimitTokens), pricing = ModelPricing(overrides = properties.pricing.mapValues { (_, v) -> ModelPricing.PriceConfig(v.inputPerMillion, v.outputPerMillion) }))`. Do NOT pass onSoftLimit (D-18 — extension point is the events Flow; apps override the whole bean if they need a callback, which @ConditionalOnMissingBean honors). Add four more @Bean methods (each taking the BudgetCircuitBreaker, and MetricsEventCollector additionally taking ModelPricing built the same way as the breaker's — or expose pricing as a separate @Bean and inject it; keep pricing override consistent with the breaker so D-11 cost is correct): `budgetEventLogger(breaker) = SLF4JEventLogger(breaker)`, `budgetMetricsCollector(breaker, ...) = MetricsEventCollector(breaker, pricing)`, `budgetEndpoint(breaker) = BudgetEndpoint(breaker)`, `budgetHealthIndicator(breaker) = BudgetBreakerHealthIndicator(breaker)`. The endpoint/health/metrics beans need NOT carry @ConditionalOnMissingBean per D-17 (only the breaker is the documented app-facing bean) but you MAY add it to the helper beans for safety — at minimum the budgetCircuitBreaker bean MUST have it. No @Component / @ComponentScan anywhere (starters must not scan). KDoc the class. Then create the registration file at `budget-breaker-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` containing the single line `io.github.unityinflow.budget.spring.BudgetBreakerAutoConfiguration` (no spring.factories — SB3 idiom).
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@AutoConfiguration" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt` returns 1
    - `grep -c "@EnableConfigurationProperties(BudgetBreakerProperties::class)" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt` returns 1
    - `grep -c "@ConditionalOnMissingBean" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt` returns >=1
    - `grep -c "onSoftLimit" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt` returns 0
    - `grep -Ec "@Component|@ComponentScan" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt` returns 0
    - `cat budget-breaker-spring-boot-starter/src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` is exactly `io.github.unityinflow.budget.spring.BudgetBreakerAutoConfiguration`
    - `./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache` exits 0
  </acceptance_criteria>
  <done>Auto-config registers the overridable breaker bean plus the four observability beans from properties, with no component scanning and no onSoftLimit wiring; discoverable via the imports file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ApplicationContextRunner unit tests (D-19 layer 1, SPRING-01)</name>
  <files>budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfigurationTest.kt</files>
  <read_first>
    - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfiguration.kt (the unit under test — bean names budgetCircuitBreaker etc.)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetCircuitBreaker.kt (no-arg constructor exists for the custom-bean test)
    - budget-breaker/src/test/kotlin/io/github/unityinflow/budget/BudgetCircuitBreakerTest.kt (test style: JUnit5 @Test, backtick names, Kotest matchers)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (Pattern 7 ApplicationContextRunner full example incl. CustomBreakerConfig, withPropertyValues, hasFailed())
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-19 layer 1: ApplicationContextRunner for auto-config conditionals + properties binding)
  </read_first>
  <behavior>
    - With defaults, the context has a single BudgetCircuitBreaker bean
    - With a user-defined BudgetCircuitBreaker bean present, the auto-config backs off (the user's bean is the one in the context)
    - With budget-breaker.soft-limit-tokens=100000 and budget-breaker.hard-limit-tokens=50000, the context fails to start
    - The endpoint, health indicator, metrics collector, and logger beans are present with defaults
  </behavior>
  <action>
    Create `BudgetBreakerAutoConfigurationTest.kt` using `org.springframework.boot.test.context.runner.ApplicationContextRunner` configured `.withConfiguration(AutoConfigurations.of(BudgetBreakerAutoConfiguration::class.java))`. Write tests with backtick names mirroring RESEARCH Pattern 7: (1) `registers BudgetCircuitBreaker with defaults` asserting `assertThat(ctx).hasSingleBean(BudgetCircuitBreaker::class.java)`; (2) `honors user-defined BudgetCircuitBreaker bean` using a private `@Configuration` inner class providing a `@Bean("customBreaker") fun customBreaker() = BudgetCircuitBreaker()` and asserting the context bean isSameAs the custom one; (3) `validates invalid property combination at startup` using `.withPropertyValues("budget-breaker.soft-limit-tokens=100000", "budget-breaker.hard-limit-tokens=50000").run { ctx -> assertThat(ctx).hasFailed() }`; (4) `registers observability beans` asserting hasSingleBean for BudgetEndpoint, BudgetBreakerHealthIndicator, MetricsEventCollector, SLF4JEventLogger. Use AssertJ `assertThat(ctx)` from spring-boot-test for context assertions; Kotest matchers may be used for value assertions. Backtick function names per project convention.
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache --tests "io.github.unityinflow.budget.spring.BudgetBreakerAutoConfigurationTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "ApplicationContextRunner" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfigurationTest.kt` returns >=1
    - `grep -c "hasFailed" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfigurationTest.kt` returns >=1
    - `grep -c "hasSingleBean" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerAutoConfigurationTest.kt` returns >=2
    - `./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache --tests "io.github.unityinflow.budget.spring.BudgetBreakerAutoConfigurationTest"` passes (all four tests green)
  </acceptance_criteria>
  <done>ApplicationContextRunner tests prove default registration, @ConditionalOnMissingBean back-off, fail-fast validation, and observability bean presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application.yml → auto-config → beans | operator config drives bean construction |
| starter jar → consuming app classpath | auto-config auto-discovered via imports file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | auto-config silently overriding a user-defined breaker | mitigate | @ConditionalOnMissingBean on budgetCircuitBreaker (D-17) — user bean always wins; covered by ApplicationContextRunner override test |
| T-02-08 | Elevation of Privilege | component-scanning in a starter jar pulling unintended beans | mitigate | No @Component / @ComponentScan; beans only via explicit @Bean in @AutoConfiguration (verified by grep gate) |
| T-02-04 | Tampering | invalid budget config bypassing limits | mitigate | BudgetBreakerProperties.init validation (plan 02) fails startup; proven by the hasFailed() test here |
</threat_model>

<verification>
- `./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache` — auto-config unit tests green
- `./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache`
- `./gradlew ktlintCheck --no-configuration-cache` after ktlintFormat
</verification>

<success_criteria>
- BudgetBreakerAutoConfiguration registers breaker (overridable) + four observability beans from properties
- imports file makes the auto-config discoverable (no spring.factories)
- SLF4JEventLogger logs only soft breaches at WARN
- ApplicationContextRunner tests green: defaults, override, fail-fast, observability beans
- No @Component, no onSoftLimit wiring, no var, no !!
</success_criteria>

<output>
Create `.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-03-SUMMARY.md` when done
</output>
