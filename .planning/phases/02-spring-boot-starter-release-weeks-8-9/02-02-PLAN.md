---
phase: 02-spring-boot-starter-release-weeks-8-9
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt
  - budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerPropertiesTest.kt
  - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt
  - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerHealthIndicator.kt
  - budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt
autonomous: true
requirements:
  - SPRING-01
  - SPRING-02
  - SPRING-03
user_setup: []

must_haves:
  truths:
    - "budget-breaker.* properties bind into an immutable Kotlin data class with startup validation"
    - "Invalid property combos (soft >= hard, non-positive) fail startup with a clear, YAML-path-named message"
    - "GET /actuator/budget returns a map of all tracked agents; GET /actuator/budget/{agentId} returns one BudgetReport"
    - "BudgetBreakerHealthIndicator always reports UP with agentsTracked/softBreaches/hardBreaches details"
    - "MetricsEventCollector subscribes to the core events Flow and registers gen_ai/budget.breaker meters"
  artifacts:
    - path: "budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt"
      provides: "@ConfigurationProperties data class for budget-breaker.* with init validation + ModelPriceProperties"
      contains: "@ConfigurationProperties"
    - path: "budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerPropertiesTest.kt"
      provides: "Plain unit tests for BudgetBreakerProperties init{} validation"
      contains: "BudgetBreakerProperties"
    - path: "budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt"
      provides: "@Endpoint(id=budget) with two @ReadOperation methods"
      contains: "@Endpoint(id = \"budget\")"
    - path: "budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerHealthIndicator.kt"
      provides: "HealthIndicator always UP with three detail keys"
      contains: "HealthIndicator"
    - path: "budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt"
      provides: "MeterBinder + SmartLifecycle collecting events into Micrometer meters"
      contains: "MeterBinder"
  key_links:
    - from: "BudgetEndpoint"
      to: "BudgetCircuitBreaker.getAllReports / getReport"
      via: "@ReadOperation reads breaker snapshot"
      pattern: "breaker\\.(getAllReports|getReport)"
    - from: "MetricsEventCollector"
      to: "BudgetCircuitBreaker.events"
      via: "scope.launch { breaker.events.collect ... }"
      pattern: "breaker\\.events\\.collect"
    - from: "MetricsEventCollector"
      to: "gen_ai.client.token.usage.input/output meters"
      via: "Counter.builder with exact OTel semconv names"
      pattern: "gen_ai\\.client\\.token\\.usage"
---

<objective>
Build the four leaf Spring components of the starter as standalone classes (no auto-config wiring yet — that is plan 03): the @ConfigurationProperties data class (SPRING-01), the Actuator endpoint + health indicator (SPRING-02), and the Micrometer MetricsEventCollector (SPRING-03). These are plain classes/beans constructed from a BudgetCircuitBreaker; they have no @Component annotations and do not self-register.

Purpose: Defining these classes before the auto-config (interface-first ordering) means plan 03 wires already-existing, already-correct types — no scavenger hunt. They consume the core APIs created in plan 01 (getAllReports, getReport, getActiveTrackerCount, getTotalSoftBreaches, getTotalHardBreaches, modelOf, CallTracked.model).

Output: Four Kotlin source files under the starter's spring package, plus a plain unit test for the properties init{} validation.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-PATTERNS.md
@.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
Symbols CREATED by this plan (new — exclude from drift/source-grounding checks):
- `BudgetBreakerProperties` (@ConfigurationProperties data class) — fields: `defaultModel`, `hardLimitTokens`, `softLimitTokens`, `pricing: Map<String, ModelPriceProperties>`
- `ModelPriceProperties` (data class) — fields: `inputPerMillion`, `outputPerMillion`
- `BudgetBreakerPropertiesTest` (test) — plain JUnit5 unit tests for init{} validation
- `BudgetEndpoint` (@Endpoint id="budget") — methods: `budget()`, `agentBudget(agentId)`
- `BudgetBreakerHealthIndicator` (HealthIndicator) — `health()`
- `MetricsEventCollector` (MeterBinder + SmartLifecycle) — `bindTo`, `start`, `stop`, `isRunning`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: BudgetBreakerProperties (@ConfigurationProperties for budget-breaker.*) + init validation unit test</name>
  <files>budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt, budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerPropertiesTest.kt</files>
  <read_first>
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/AgentBudget.kt (analog: val-only data class with init require() validation, defaults claude-sonnet-4-6 / 100_000 / 80_000)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/ModelPricing.kt (PriceConfig shape: inputPerMillion, outputPerMillion)
    - budget-breaker/src/test/kotlin/io/github/unityinflow/budget/BudgetCircuitBreakerTest.kt (test style: JUnit5 @Test, backtick names, Kotest matchers, shouldThrow)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (Pattern 2: @ConfigurationProperties Kotlin data class — constructor binding native in SB3, no @ConstructorBinding)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-01 prefix budget-breaker.*; D-02 knobs default-model/soft-limit-tokens/hard-limit-tokens/pricing; D-04 zero-config defaults + fail-fast on invalid combos)
  </read_first>
  <behavior>
    - Default construction yields defaultModel="claude-sonnet-4-6", hardLimitTokens=100_000, softLimitTokens=80_000, pricing=emptyMap()
    - Constructing with softLimitTokens=100_000 and hardLimitTokens=50_000 throws IllegalArgumentException whose message contains "soft-limit-tokens" and "hard-limit-tokens"
    - Constructing with hardLimitTokens=-1 or softLimitTokens=0 throws IllegalArgumentException
  </behavior>
  <action>
    Create `BudgetBreakerProperties.kt` in package `io.github.unityinflow.budget.spring`. Annotate the class with `@ConfigurationProperties("budget-breaker")` (D-01 — prefix is budget-breaker, NOT kore.budget). Declare it as a `val`-only data class with parameters: `defaultModel: String = "claude-sonnet-4-6"`, `hardLimitTokens: Long = 100_000`, `softLimitTokens: Long = 80_000`, `pricing: Map<String, ModelPriceProperties> = emptyMap()`. Copy the three `require(...)` checks from AgentBudget's init block but write the messages with YAML-path property names: "budget-breaker.hard-limit-tokens must be positive", "budget-breaker.soft-limit-tokens must be positive", "budget-breaker.soft-limit-tokens must be <= hard-limit-tokens". Add a nested/sibling `data class ModelPriceProperties(val inputPerMillion: Double = 0.0, val outputPerMillion: Double = 0.0)`. KDoc on the class and each property (D-02 semantics). Use constructor binding — do NOT add @ConstructorBinding (not needed in SB3). No @Component, no @EnableConfigurationProperties here (that lands in the auto-config in plan 03).

    Then create `BudgetBreakerPropertiesTest.kt` — a plain JUnit5 unit test (NOT a Spring context test; the ApplicationContextRunner binding test lives in plan 03). This is what justifies tdd="true" on this task. Write backtick-named tests asserting the three behaviors above by constructing `BudgetBreakerProperties(...)` directly: (1) default construction yields the documented defaults (Kotest shouldBe); (2) `shouldThrow<IllegalArgumentException> { BudgetBreakerProperties(softLimitTokens = 100_000, hardLimitTokens = 50_000) }` and assert the message contains both "soft-limit-tokens" and "hard-limit-tokens"; (3) shouldThrow for hardLimitTokens=-1 and for softLimitTokens=0. This proves the init{} require() logic without booting Spring.
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache --tests "io.github.unityinflow.budget.spring.BudgetBreakerPropertiesTest"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@ConfigurationProperties(\"budget-breaker\")" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt` returns 1
    - `grep -c "data class ModelPriceProperties" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt` returns 1
    - `grep -c "var " budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt` returns 0
    - `grep "must be <= hard-limit-tokens" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerProperties.kt` matches
    - `grep -c "shouldThrow" budget-breaker-spring-boot-starter/src/test/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerPropertiesTest.kt` returns >=2
    - `./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache --tests "io.github.unityinflow.budget.spring.BudgetBreakerPropertiesTest"` passes
  </acceptance_criteria>
  <done>Immutable @ConfigurationProperties data class binds budget-breaker.* with fail-fast init validation and a pricing override map; a plain unit test proves the init{} validation (RED→GREEN) without a Spring context.</done>
</task>

<task type="auto">
  <name>Task 2: BudgetEndpoint + BudgetBreakerHealthIndicator (SPRING-02)</name>
  <files>budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt, budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerHealthIndicator.kt</files>
  <read_first>
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetCircuitBreaker.kt (post-plan-01: getAllReports(): Map<String, BudgetSnapshot>, getReport(agentId): BudgetReport?, getActiveTrackerCount(), getTotalSoftBreaches(), getTotalHardBreaches())
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetSnapshot.kt (BudgetSnapshot(report, running) created in plan 01)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetReport.kt (report fields)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (Pattern 3 @Endpoint/@ReadOperation/@Selector; Pattern 4 HealthIndicator always UP; Pitfall 5 @Endpoint needs @Bean; anti-pattern: never block in @ReadOperation; D-07: never DOWN)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-05 endpoint shape; D-06 read-only GET; D-07 health detail keys agentsTracked/softBreaches/hardBreaches; D-08 running flag)
  </read_first>
  <action>
    Create `BudgetEndpoint.kt`: class annotated `@Endpoint(id = "budget")` with constructor param `private val breaker: BudgetCircuitBreaker`. Add `@ReadOperation fun budget(): Map<String, BudgetSnapshot> = breaker.getAllReports()` (returns all tracked agents incl. in-flight, running flag from plan-01 snapshot — D-05/D-08). Add `@ReadOperation fun agentBudget(@Selector agentId: String): BudgetReport? = breaker.getReport(agentId)` (per-agent full report — D-05). GET-only, no @WriteOperation/@DeleteOperation (D-06). Never block: both methods only read ConcurrentHashMap snapshots from the breaker — no suspend, no runBlocking. KDoc the class documenting the exposure requirement (`management.endpoints.web.exposure.include=budget`) and both routes. Do NOT add @Component (Pitfall 5 — it will be a @Bean in the auto-config in plan 03). Create `BudgetBreakerHealthIndicator.kt`: class implementing `org.springframework.boot.actuate.health.HealthIndicator` with constructor `private val breaker: BudgetCircuitBreaker`. `override fun health(): Health` returns `Health.up().withDetail("agentsTracked", breaker.getActiveTrackerCount()).withDetail("softBreaches", breaker.getTotalSoftBreaches()).withDetail("hardBreaches", breaker.getTotalHardBreaches()).build()`. ALWAYS Health.up() regardless of breach state (D-07 — a breach must never flip to DOWN or k8s probes kill healthy pods). Detail keys exactly agentsTracked, softBreaches, hardBreaches. KDoc noting the always-UP contract and rationale. No @Component.
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "@Endpoint(id = \"budget\")" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt` returns 1
    - `grep -c "@ReadOperation" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt` returns 2
    - `grep -Ec "@WriteOperation|@DeleteOperation" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetEndpoint.kt` returns 0
    - `grep -c "Health.up()" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerHealthIndicator.kt` returns 1 and `grep -c "Health.down" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerHealthIndicator.kt` returns 0
    - `grep -Eo "agentsTracked|softBreaches|hardBreaches" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/BudgetBreakerHealthIndicator.kt | sort -u | wc -l` returns 3
    - `./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache` exits 0
  </acceptance_criteria>
  <done>Read-only @Endpoint(id=budget) with aggregate + per-agent operations, and an always-UP HealthIndicator with the three required detail keys; both are plain classes (no @Component).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: MetricsEventCollector (MeterBinder + SmartLifecycle, SPRING-03)</name>
  <files>budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt</files>
  <read_first>
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetEvent.kt (post-plan-01: CallTracked now has model; SoftLimitReached, HardLimitExceeded variants)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/BudgetCircuitBreaker.kt (events: SharedFlow<BudgetEvent>; modelOf(agentId); getAllReports for ratio gauge source)
    - budget-breaker/src/main/kotlin/io/github/unityinflow/budget/TokenTracker.kt (AtomicLong mutable-state-without-var pattern)
    - budget-breaker/src/test/kotlin/io/github/unityinflow/budget/BudgetCircuitBreakerTest.kt (Flow.collect subscription pattern, lines 62-81)
    - ../06-token-dashboard/src/main/kotlin/io/github/unityinflow/tokendashboard/otlp/OtlpMetricMapper.kt (CONSUMER contract: recognizes exactly "gen_ai.client.token.usage.input" and "gen_ai.client.token.usage.output" — these exact strings)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-RESEARCH.md (Pattern 5 MeterBinder; Pattern 6 SmartLifecycle coroutine scope; Code Examples: counter + gauge registration; Pitfall 6 collector-before-events; anti-patterns: no GlobalScope, no MeterRegistry in constructor)
    - .planning/phases/02-spring-boot-starter-release-weeks-8-9/02-CONTEXT.md (D-09 metric names; D-10 collector subscribes to events Flow not inline; D-11 cost.usd via ModelPricing)
  </read_first>
  <behavior>
    - After bindTo(registry) + start(), emitting CallTracked(agent="a", model="gpt-4o", promptTokens=10, completionTokens=5) increments counter "gen_ai.client.token.usage.input"{agent=a,model=gpt-4o} by 10 and "gen_ai.client.token.usage.output" by 5
    - Emitting SoftLimitReached(agent="a") increments "budget.breaker.breach"{agent=a,type=soft} by 1; HardLimitExceeded increments {type=hard}
    - A CallTracked also increments "budget.breaker.cost.usd"{agent,model} by the ModelPricing-computed cost
    - stop() cancels the coroutine scope (isRunning() becomes false); no leak after context shutdown
  </behavior>
  <action>
    Create `MetricsEventCollector.kt` implementing both `io.micrometer.core.instrument.binder.MeterBinder` and `org.springframework.context.SmartLifecycle`. Constructor: `private val breaker: BudgetCircuitBreaker`, `private val pricing: ModelPricing` (pass the same pricing the breaker uses so application.yml overrides are honored — D-11). Hold `private val scope = CoroutineScope(SupervisorJob() + Dispatchers.Default)` (Pattern 6 — never GlobalScope).

    CLAUDE.md forbids `var`: RESEARCH Pattern 5/6 sample code uses `var running = false` and a mutable `var registry` assignment — DO NOT copy those mutable assignments. Instead, model the lifecycle running flag as `private val running = AtomicBoolean(false)` and the registry reference as `private val registryRef = AtomicReference<MeterRegistry?>(null)`. `override fun bindTo(registry: MeterRegistry)` does `registryRef.set(registry)`; do not start collecting here. `override fun start()` does `running.set(true)` and launches `scope.launch { breaker.events.collect { event -> when(event) { ... } } }` with an EXHAUSTIVE when over the three BudgetEvent variants (no else). Read the registry inside the collector via `registryRef.get() ?: return@collect` (no-op if unbound; never `!!`).

    For CallTracked: register/increment `Counter.builder("gen_ai.client.token.usage.input").tags("agent", event.agentId, "model", event.model).register(registry).increment(event.promptTokens.toDouble())` and the `.output` counter for completionTokens, plus `Counter.builder("budget.breaker.cost.usd").tags("agent", event.agentId, "model", event.model)...increment(pricing.estimateCost(event.model, event.promptTokens, event.completionTokens))`. Use the EXACT metric names "gen_ai.client.token.usage.input" / "gen_ai.client.token.usage.output" (underscore in gen_ai, dots elsewhere) — these are the strings OtlpMetricMapper.kt matches; do not rename. For SoftLimitReached: increment `Counter.builder("budget.breaker.breach").tags("agent", event.agentId, "type", "soft")`. For HardLimitExceeded: same counter with type="hard". Register a `budget.breaker.usage.ratio` Gauge per agent (used/hard) — drive it from an AtomicDouble-style holder kept in a `ConcurrentHashMap<String, AtomicReference<Double>>` (or `java.util.concurrent.atomic.DoubleAdder`-style holder) so the gauge holds a strong reference (RESEARCH gauge example); update the holder on CallTracked using breaker snapshot percentUsed/100.0 or event.tokensUsed over the agent's hard limit. Never use `var` — all mutable state lives in Micrometer meters or in Atomic holders; never use `!!` on the registry (guard with the AtomicReference + `?:` fallback above). `override fun stop()` does `running.set(false)` and `scope.cancel()`. `override fun isRunning(): Boolean = running.get()`. KDoc the class explaining the MeterBinder+SmartLifecycle lifecycle and the exact metric naming contract with token-dashboard. No @Component.
  </action>
  <verify>
    <automated>./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "gen_ai.client.token.usage.input" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt` returns >=1 AND `grep -c "gen_ai.client.token.usage.output" ...` returns >=1
    - `grep -c "budget.breaker.breach" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt` returns >=1
    - `grep -c "budget.breaker.cost.usd" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt` returns >=1
    - `grep -Ec "MeterBinder|SmartLifecycle" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt` returns >=2
    - `grep -c "GlobalScope" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt` returns 0 AND `grep -c "var " ...` returns 0 AND `grep -c "!!" ...` returns 0
    - `grep -Ec "AtomicBoolean|AtomicReference" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt` returns >=2
    - `grep -c "breaker.events.collect" budget-breaker-spring-boot-starter/src/main/kotlin/io/github/unityinflow/budget/spring/MetricsEventCollector.kt` returns 1
    - `./gradlew :budget-breaker-spring-boot-starter:compileKotlin --no-configuration-cache` exits 0
  </acceptance_criteria>
  <done>MetricsEventCollector binds Micrometer meters with OTel-semconv names recognized by token-dashboard, subscribes to the core Flow, and manages its coroutine scope via SmartLifecycle; running state via AtomicBoolean and registry via AtomicReference; no GlobalScope, no var, no !!.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| application.yml → BudgetBreakerProperties | untrusted operator config binds into typed properties |
| management HTTP → BudgetEndpoint | actuator endpoint may expose agent names + token counts |
| core SharedFlow → starter collectors | in-process event stream, trusted producer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Information Disclosure | GET /actuator/budget exposing agent names + token counts | mitigate | Endpoint is NOT auto-exposed; consumer must opt in via management.endpoints.web.exposure.include (documented in plan 04 README). Read-only (D-06) — no mutation surface. |
| T-02-04 | Tampering | budget limit bypass via property injection (dev/test profile) | mitigate | BudgetBreakerProperties.init { require(...) } fails startup on soft>=hard or non-positive values; proven by BudgetBreakerPropertiesTest here |
| T-02-05 | Denial of Service | coroutine scope not cancelled on context shutdown (resource leak) | mitigate | MetricsEventCollector.stop() (SmartLifecycle) cancels SupervisorJob scope; never GlobalScope |
| T-02-06 | Information Disclosure | HealthIndicator leaking breach state to flip health | accept (by design) | D-07: health stays UP and only contributes counts as details; breach counts in /actuator/health are acceptable operational detail, not sensitive secrets |
</threat_model>

<verification>
- `./gradlew :budget-breaker-spring-boot-starter:test --no-configuration-cache` — properties unit test + compile of all four files against core + Spring + Micrometer
- `./gradlew ktlintCheck --no-configuration-cache` after ktlintFormat
- Metric name strings cross-checked against ../06-token-dashboard OtlpMetricMapper.kt consumer
</verification>

<success_criteria>
- BudgetBreakerProperties binds budget-breaker.* with fail-fast validation, proven by a plain unit test
- BudgetEndpoint exposes read-only aggregate + per-agent operations
- BudgetBreakerHealthIndicator always UP with three detail keys
- MetricsEventCollector registers all five meter families with exact OTel-semconv names
- No @Component on any class (wiring is plan 03's auto-config)
- ktlint clean; no var, no !!, exhaustive when over BudgetEvent; running via AtomicBoolean, registry via AtomicReference
</success_criteria>

<output>
Create `.planning/phases/02-spring-boot-starter-release-weeks-8-9/02-02-SUMMARY.md` when done
</output>
</content>
